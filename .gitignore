# Strictly Private Assignment Source Materials
material/
*.pdf
*.docx
*.txt

# Google API Secrets & Telemetry (Zero-Tolerance Security)
credentials.json
token.json
.env
*.pem
*.key

# uv / Python / IDE Virtual Environments
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
.vscode/
.cursor/
coverage.xml
private-google-client.json
token.json
data/

# match-setup: keep the __UNFILLED templates; ignore the filled copies (they hold live tokens)
match_setup/OUR_DETAILS.txt
match_setup/OPPONENT_DETAILS.txt

# userland binaries (cloudflared, etc.) — never commit 30MB+ blobs
bin/
